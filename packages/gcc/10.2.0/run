#!/usr/bin/env bash

# only works for cpp
# rename 's/$/\.cpp/' "$@" # Add .cpp extension
# ^ why not just pass .cpp in the names lol

g++ -std=c++17 *.cpp

chmod +x a.out

shift # Discard main filename
shift # Discard second filename
./a.out "$@"

